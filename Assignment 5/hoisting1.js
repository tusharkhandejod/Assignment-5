let a = 100;
let b = 20;

sum(a, b)
substract(a, b)
product(a, b)
divide(a, b)

function sum(a, b) {
    console.log("\nSum is : " + (a + b))
}

function substract(a, b) {
    console.log("\nSubstraction is : " + (a - b))
}

function product(a, b) {
    console.log("\nProduct is : " + (a * b))
}

function divide(a, b) {
    console.log("\nDivision is : " + (a / b))
}